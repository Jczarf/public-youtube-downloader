{
  "name": "mediaflow-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build:web": "esbuild web/frontend.js --bundle --format=esm --platform=browser --target=es2022 --minify --outfile=web/static/app.js"
  },
  "dependencies": {
    "mediabunny": "1.56.1"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
